{
  "name": "sciagent-zotero-addon",
  "version": "0.1.1",
  "private": true,
  "description": "SciAgent native Zotero 9 add-on scaffold and MVP UI",
  "type": "module",
  "scripts": {
    "clean": "node ./scripts/build.mjs clean",
    "lint": "biome lint .",
    "build": "node ./scripts/build.mjs build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "adm-zip": "^0.5.16",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "zotero-types": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
